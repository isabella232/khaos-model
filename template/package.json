{
  "name": "mongodb-{{slugcase name}}-model",
  "description": "{{description}}",
  "author": "{{author_name}} <{{author_email}}>",
  "version": "0.0.0",
  "bugs": "https://github.com/mongodb-js/mongodb-{{slugcase name}}-model/issues",
  "homepage": "https://github.com/mongodb-js/mongodb-{{slugcase name}}-model",
  "repository": "git://github.com/mongodb-js/mongodb-{{slugcase name}}-model.git",
  "license": "Apache-2.0",
  "keywords": [
    "mongodb-js"
  ],
  "scripts": {
    "ci": "mocha",
    "test": "mocha",
    "check": "mongodb-js-precommit"
  },
  "precommit": ["check", "test"],
  "dependencies": {
    "ampersand-model": "^5.0.3",
    "ampersand-rest-collection": "^4.0.0"
  },
  "devDependencies": {
    "eslint-config-mongodb-js": "^0.1.5",
    "mocha": "^2.2.5",
    "mongodb-js-precommit": "^0.1.4",
    "pre-commit": "^1.1.1"
  }
}
