var assert = require('assert');
var {{titlecase name}} = require('../');

describe('mongodb-{{slugcase name}}-model', function() {
  it('should work', function(){
    assert({{titlecase name}});
  });
});
